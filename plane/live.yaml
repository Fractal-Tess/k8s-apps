apiVersion: apps/v1
kind: Deployment
metadata:
  name: live
  namespace: plane
  labels:
    app: live
spec:
  replicas: 1
  selector:
    matchLabels:
      app: live
  template:
    metadata:
      labels:
        app: live
    spec:
      initContainers:
        - name: wait-for-api
          image: busybox:1.36
          command:
            - /bin/sh
            - -c
            - |
              until nc -z api 8000; do sleep 2; done
              until nc -z web 3000; do sleep 2; done
              until nc -z plane-redis 6379; do sleep 2; done
              sleep 3
      containers:
        - name: live
          image: artifacts.plane.so/makeplane/plane-live:v1.0.0
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: plane-secrets
          livenessProbe:
            exec:
              command:
                - echo
                - "hey whats up"
            initialDelaySeconds: 10
            periodSeconds: 2
            timeoutSeconds: 10
            failureThreshold: 15
          readinessProbe:
            exec:
              command:
                - echo
                - "hey whats up"
            initialDelaySeconds: 5
            periodSeconds: 2
            timeoutSeconds: 10
            failureThreshold: 15
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: live
  namespace: plane
spec:
  selector:
    app: live
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
